import { Zen_Loop, Nanum_Gothic } from 'next/font/google'

export const zenFont = Zen_Loop({
    weight: [ '400'],
    subsets: ['latin'],
    display: 'swap',
});

export const nanumFont = Nanum_Gothic({
    weight: ['400'],
    subsets: ['latin'],
    display: 'swap',
});